<!doctype html>
<html lang="en">
<head>
<style>
label {
  font-family: "Jazz LET", fantasy;
  font-size: 20px;
  color: #00008B;
}
div {
  font-family: "Lucida Console", "Courier New", monospace;
  font-size: 15px;
  color: #00008B;
}
</style>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>Lisaa arviointi</title>
	<link href= "/public/stylesheets/arviointi/style.css" rel="stylesheet" type="text/css" />
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

	<% if (messages.error) { %>
	<div class="alert alert-danger" role="alert"><%- messages.error %></div>
	<% } %>

    <div class="card"> 
    	<div class="card-header">
			Lisää arviointi
		</div>
        <div class="card-body">
            <form action="/arviointi/add" method="post">
                <div class="form-group">
                    <label>Paivamaara:</label>
                    <input type="text" class="form-control" name="Paivamaara" value="<%= Paivamaara %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>Arvosana:</label>
                    <input type="text" class="form-control" name="Arvosana" value="<%= Arvosana %>" autocomplete="off">
                </div>
	 <div class="form-group">
                    <label>idOpiskelija:</label>
                    <input type="text" class="form-control" name="idOpiskelija" value="<%= idOpiskelija %>" autocomplete="off">
                </div>
	 <div class="form-group">
                    <label>idOpintojakso:</label>
                    <input type="text" class="form-control" name="idOpintojakso" value="<%= idOpintojakso %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-info" value="Add"/>
                </div>
            </form>
        </div>
    </div>

</body>
</html>